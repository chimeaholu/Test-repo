# Agrodomain Rolling Review — B-047, B-020, and B-021

Date: 2026-04-13
Reviewer: `engineering`
Scope:
- `B-047` implementation commit `89e96728`
- `B-020` implementation commit `d1624a9e`
- `B-021` implementation commit `7436a378`

## Signoff

Overall: `PASS`

## Findings

No blocking findings were identified in the reviewed diffs.

## Review Notes

- `B-047` stays inside backlog scope:
  - turns the `B-046` envelope into a versioned ingest seam without pretending there is a live transport service or event bus already wired
  - keeps idempotency and resumability explicit at the API profile layer instead of leaking them into the sensor envelope contract
  - reuses the `B-035` tool-contract lane rather than inventing a second schema discipline
- `B-020` stays inside backlog scope:
  - models the partner boundary as an adapter contract with liability ownership, dispute path, and policy-envelope checks
  - keeps the platform in the normalization/orchestration role instead of pretending Agrodomain is the regulated underwriter
  - remains deterministic and replay-safe for later HITL and console work
- `B-021` stays inside backlog scope:
  - binds payout events to approved insurance decisions rather than letting raw climate signals trigger settlement-adjacent effects on their own
  - persists explicit source references and thresholds for each payout decision
  - leaves operator-console review and downstream notifications to later beads

## Residual Risks

- Wave 2.7 still lacks the event bus partition model and governance boundary (`B-048`, `B-049`).
- Wave 3 still lacks the finance/insurance HITL console and downstream delivery/traceability continuation (`B-022`, `B-023`, `B-024`).
- Step `12` browser evidence, approval/acceptance signoff, and Agent Mail coordination evidence remain late-stage SOP gaps outside this cycle.

## Conclusion

Wave 2.7 and Wave 3 can continue without re-planning. The IoT ingest seam is now complete through `B-047`, and the climate-finance lane now extends from normalized signals into partner decisions and trigger-based payout events.
