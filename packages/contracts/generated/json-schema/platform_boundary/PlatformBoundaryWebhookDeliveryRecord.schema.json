{
  "$ref": "#/definitions/PlatformBoundaryWebhookDeliveryRecord",
  "definitions": {
    "PlatformBoundaryWebhookDeliveryRecord": {
      "additionalProperties": false,
      "properties": {
        "aggregate_id": {
          "maxLength": 80,
          "minLength": 1,
          "type": "string"
        },
        "delivery_id": {
          "maxLength": 80,
          "minLength": 1,
          "type": "string"
        },
        "delivery_mode": {
          "enum": [
            "webhook"
          ],
          "type": "string"
        },
        "delivery_target": {
          "format": "uri",
          "maxLength": 240,
          "type": "string"
        },
        "event_family": {
          "maxLength": 120,
          "minLength": 3,
          "type": "string"
        },
        "partner_slug": {
          "maxLength": 80,
          "minLength": 2,
          "type": "string"
        },
        "queued_at": {
          "description": "ISO-8601 timestamp with timezone offset",
          "format": "date-time",
          "type": "string"
        },
        "schema_version": {
          "enum": [
            "2026-04-18.wave1"
          ],
          "type": "string"
        },
        "status": {
          "enum": [
            "queued",
            "published"
          ],
          "type": "string"
        }
      },
      "required": [
        "schema_version",
        "delivery_id",
        "partner_slug",
        "event_family",
        "aggregate_id",
        "delivery_mode",
        "delivery_target",
        "status",
        "queued_at"
      ],
      "type": "object"
    }
  },
  "description": "Webhook queue receipt with partner, delivery target, and queue status.",
  "x-agrodomain-contract": {
    "id": "platform_boundary.webhook_delivery_record",
    "domain": "platform_boundary",
    "kind": "response",
    "schema_version": "2026-04-18.wave1",
    "traceability": [
      "DI-004"
    ],
    "source_artifacts": [
      "output_to_user/AGRODOMAIN-ENHANCEMENT-BUILD-SPEC.md"
    ]
  }
}
